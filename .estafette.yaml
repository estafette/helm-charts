builder:
  track: dev

labels:
  team: estafette-team
  language: ruby

version:
  semver:
    major: 0
    minor: 0

stages:
  validate-github-pages-build:
    image: ruby:2.6.5-stretch
    commands:
    - bundle install --gemfile=./Gemfile
    - bundle exec jekyll build